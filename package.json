{
  "scripts": {
    "start": "live-server . --port=4000 --host=localhost --no-browser",
    "format": "prettier --write .",
    "dev": "vite"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "live-server": "^1.2.2",
    "prettier": "^3.7.4",
    "vite": "^7.3.1"
  }
}
